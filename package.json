{
  "scripts": {
    "prebuild": "npx rimraf out",
    "build": "npm run prebuild && npx tsc",
    "run": "npm run build && node out/main.js"
  },
  "devDependencies": {
    "@tsconfig/node20": "1.0.1",
    "rimraf": "5.0.1",
    "typescript": "5.1.3"
  }
}
